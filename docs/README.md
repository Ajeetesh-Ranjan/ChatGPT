# AegisAccess Starter

AegisAccess is a combined API and SPA starter focused on application management and access reviews. It includes:

- Express + SQLite API with audit logging
- React + Vite SPA with ORR-aligned application capture
- GitHub Actions pipeline with tests and DAST hook for staging
