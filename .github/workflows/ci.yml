name: CI

on:
  push:
<<<<<<< HEAD
    branches: ["main", "work", "master"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm run test

  build-web:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      - name: Build web SPA
        run: npm run build --workspace web

  dast:
    runs-on: ubuntu-latest
    needs: build-web
    if: github.ref == 'refs/heads/work' || github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Run ZAP baseline scan (staging)
        env:
          TARGET: ${STAGING_URL}
        run: |
          if [ -z "$TARGET" ]; then
            echo "STAGING_URL not set; skipping ZAP baseline" && exit 0;
          fi
          docker run --rm -v $(pwd):/zap/wrk -t owasp/zap2docker-stable zap-baseline.py -t "$TARGET" -r zap-report.html
      - name: Upload ZAP report
        if: env.TARGET != ''
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: zap-report.html
=======
    branches: ["main", "master", "develop"]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install API deps
        run: |
          cd api
          npm ci

      - name: Install SPA deps
        run: |
          cd web
          npm ci

      - name: Build API
        run: |
          cd api
          npm run build

      - name: Test API
        run: |
          cd api
          npm test

      - name: Build SPA
        run: |
          cd web
          npm run build

      - name: Test SPA
        run: |
          cd web
          npm test

      - name: OWASP ZAP Baseline (staging)
        if: env.STAGING_URL != ''
        uses: zaproxy/action-baseline@v0.12.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target: ${{ env.STAGING_URL }}
          rules_file_name: '.zap/rules.tsv'
        env:
          STAGING_URL: ${{ secrets.STAGING_URL || '' }}
>>>>>>> origin/main
